name: Weekly Netlify Deploy

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

jobs:
  trigger-netlify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Build
        run: |
          curl -X POST -d {} https://api.netlify.com/build_hooks/690bd73e3790563eff8d3067
